{
  "name": "typescript-template",
  "version": "0.0.0",
  "description": "Typescript template for nodejs",
  "main": "index.js",
  "scripts": {
    "start": "npm run dev",
    "build": "tsc",
    "dev": "ts-node src/index.ts",
    "prod": "npm run build && node dist/index.js --production",
    "test": "npm run build && jasmine --reporter=jasmine-console-reporter --config=jasmine.json"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/jasmine": "^3.7.7",
    "@types/node": "^15.12.4",
    "jasmine": "^3.7.0",
    "jasmine-console-reporter": "^3.1.0",
    "ts-node": "^9.0.0",
    "typescript": "^4.0.3"
  }
}
